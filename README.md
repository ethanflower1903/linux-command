# linux notes
#linux commands


...mm//
ls        # Bulunduğun dizindeki dosyaları listeler
ls -la    # Gizli dosyalar + detaylı liste
pwd       # Şu an hangi dizindeyim
cd        # Dizin değiştir
cd ..     # Bir üst dizin
mkdir     # Dizin oluştur
rmdir     # Boş dizin sil
cp        # Kopyala
mv        # Taşı / yeniden adlandır
rm        # Sil (DİKKATLİ!)
rm -rf    # Zorla + recursive siler (çok tehlikeli)
cat       # Dosya içeriğini göster
less      # Sayfa sayfa göster (çok önemli)
more      # less’in eski hali
head      # Dosyanın ilk satırları
tail      # Dosyanın son satırları
tail -f   # Log takibi (canlı izleme)
file      # Dosya türünü söyler
find      # Dosya/dizin arama
grep      # Metin içinde arama
grep -i   # Büyük/küçük harf duyarsız
grep -r   # Recursive arama
awk       # Kolon bazlı işlem
sed       # Metin düzenleme
wc        # Satır / kelime / karakter sayısı
chmod     # Dosya izinleri
chown     # Sahip değiştirme
whoami   # Ben kimim
id        # Kullanıcı bilgileri
sudo      # Yetkili çalıştır
su        # Başka kullanıcıya geç
top       # Canlı sistem durumu
htop      # Daha gelişmiş top (önerilir)
ps        # Çalışan prosesler
df -h     # Disk kullanımı
du -sh    # Dizin boyutu
free -h   # RAM durumu
uptime   # Sistem ne zamandır açık
ip a      # IP adresleri
ip r      # Routing tablosu
ping      # Bağlantı testi
netstat   # Portlar (eski)
ss        # Portlar (yeni)
curl      # HTTP istekleri
wget      # Dosya indirme
nc        # Netcat (çok kritik)
tar -xvf   # Aç
tar -cvf   # Oluştur
zip
unzip
gzip
gunzip
history   # Komut geçmişi
clear     # Ekranı temizle
alias     # Kısayol tanımla
export    # Ortam değişkeni
nmap
tcpdump
wireshark
msfconsole
john
hashcat
ls, cd, pwd, cp, mv, rm
cat, less, tail -f
grep, find
chmod, sudo
ip a, ss

# cssmm
